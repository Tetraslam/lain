import type { LainExtension } from "@lain/shared";

/**
 * Freeform extension — the default. No constraints, pure divergent thinking.
 * Serves as the no-op reference implementation.
 */
export const freeformExtension: LainExtension = {
  name: "freeform",
  version: "0.1.0",
};
